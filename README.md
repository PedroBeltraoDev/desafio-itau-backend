🏆 Desafio Técnico Backend Júnior Itaú - API de Transações e EstatísticasEste repositório apresenta a solução implementada para o desafio técnico de vaga Backend Júnior do Itaú. O objetivo foi construir uma API RESTful que gerencie o registro de transações financeiras e forneça estatísticas em tempo real sobre os dados, seguindo estritas restrições de arquitetura.🌟 Visão Geral da SoluçãoO principal desafio foi implementar um sistema que lida com dados em tempo real sem utilizar qualquer banco de dados ou camada de persistência.A solução utiliza Java e Spring Boot para:Receber e validar transações via um endpoint POST.Armazenar as transações exclusivamente na memória da aplicação.Calcular estatísticas agregadas (soma, média, min, max, count) sobre transações ocorridas nos últimos 60 segundos.Garantir a thread-safety e o alto desempenho no armazenamento e recuperação dos dados.🛠️ Tecnologias UtilizadasCategoriaTecnologiaDetalhesLinguagemJavaVersão 17+FrameworkSpring BootUtilizado para criar a aplicação e configurar a API REST.DependênciasSpring WebPara criar os Controllers e endpoints REST.FerramentaMavenGerenciador de dependências e build.Estrutura de DadosConcurrentLinkedQueueEstrutura thread-safe escolhida para armazenar as transações em memória.🧭 Endpoints da APIA API está exposta na porta 8080 (configuração padrão do Spring Boot). O path base da aplicação é http://localhost:8080.MétodoEndpointDescriçãoCorpo da RequisiçãoStatus de RetornoPOST/transacoesRegistra uma nova transação, validando valor e data/hora.TransactionRequest201 Created, 400 Bad Request, ou 422 Unprocessable EntityGET/estatisticasRetorna as estatísticas calculadas sobre transações dos últimos 60 segundos.(Nenhum)200 OKDELETE/transacoesLimpa completamente todas as transações armazenadas na memória.(Nenhum)200 OKDetalhe do Endpoint: /transacoes (POST)Corpo (JSON de Exemplo):JSON{
  "valor": 123.45,
  "dataHora": "2025-11-05T20:55:00.000Z" 
}
Validações (Retorno 422 Unprocessable Entity):A transação não deve ter ocorrido no futuro.O valor deve ser igual ou maior que zero.Detalhe do Endpoint: /estatisticas (GET)Corpo (JSON de Resposta - Exemplo):JSON{
  "soma": 587.90,
  "media": 117.58,
  "max": 300.00,
  "min": 15.00,
  "count": 5
}
Lógica de Negócio:As estatísticas são calculadas a partir de todas as transações que possuem uma dataHora de ocorrência dentro dos últimos 60 segundos do momento da requisição.💻 Estrutura do Projeto e ArquiteturaO projeto foi estruturado em camadas seguindo o padrão recomendado para aplicações Spring Boot:model: Contém a classe principal de domínio (Transaction).dto: Contém as classes de DTO (Data Transfer Objects), como TransactionRequest (para a entrada de dados com validações) e StatisticsResponse (para o retorno das estatísticas).service: Contém a regra de negócio (TransactionService). É aqui que o armazenamento em memória e a lógica de filtragem/cálculo das estatísticas ocorrem.controller: Contém as classes REST Controller (TransactionController e StatisticsController), responsáveis por receber as requisições e delegar para a camada de serviço.Ponto Chave: Gerenciamento em MemóriaPara aderir ao requisito de não usar banco de dados ou cache, a classe TransactionService utiliza uma instância de ConcurrentLinkedQueue<Transaction> para armazenar as transações.Vantagem: É uma fila thread-safe e otimizada para operações add e poll concorrentes, crucial em um ambiente multithreaded como o Spring Boot.Cálculo de Estatísticas: A funcionalidade de cálculo utiliza a API Streams do Java 8+ e o método summarizingDouble(Transaction::getValor) para aplicar a regra de filtro (últimos 60 segundos) e gerar o DoubleSummaryStatistics de forma eficiente.⚙️ Como Executar a AplicaçãoPré-requisitosCertifique-se de ter o Java Development Kit (JDK) 17 ou superior instalado.PassosClone o repositório:Bashgit clone [SEU_LINK_DO_REPOSITÓRIO]
cd [NOME_DO_SEU_REPOSITÓRIO]
Compile e execute o projeto:O Spring Boot Maven Plugin facilita a execução:Bash./mvnw spring-boot:run
Teste os endpoints:Use ferramentas como Postman, Thunder Client ou cURL para interagir com a API em http://localhost:8080.
